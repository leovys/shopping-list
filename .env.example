# Copie este arquivo para .env.local e preencha os valores
# Nunca commite o .env.local!

# MongoDB Atlas — pegue em: Database > Connect > Connect your application
MONGODB_URI=mongodb+srv://USUARIO:SENHA@CLUSTER.mongodb.net/shopping_list?retryWrites=true&w=majority

# Chave secreta para JWT — use uma string longa e aleatória
JWT_SECRET=troque_por_uma_chave_longa_e_aleatoria_aqui
